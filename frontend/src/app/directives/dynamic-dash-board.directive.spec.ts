import { DynamicDashBoardDirective } from './dynamic-dash-board.directive';

describe('DynamicDashBoardDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicDashBoardDirective();
    expect(directive).toBeTruthy();
  });
});
